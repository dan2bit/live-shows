# Temporary placeholder

This file exists only to give the `feature/issue-10-recommend-debug` branch a
commit that diverges from `main`, so the branch is selectable and a PR can be
opened. Delete it in the same commit where `index.html` (recommendation +
debug-mode preview, issue #10) is checked in.
